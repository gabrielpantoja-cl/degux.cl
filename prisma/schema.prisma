// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

datasource db {
  provider = "postgresql"
  url      = env("POSTGRES_PRISMA_URL")
}

generator client {
  provider = "prisma-client-js"
}

model referencialesTable {
  id               String        @id @default(uuid())
  colaborador_id   String
  colaborador      Colaboradores @relation(fields: [colaborador_id], references: [id])
  lat              Float
  lng              Float
  fojas            String
  numero           String
  anio             Int
  cbr              String
  comprador        String
  vendedor         String
  predio           String
  comuna           String
  rol              String
  fechaDeEscritura DateTime
  superficie       Float
  monto            Int
  status           String
  observaciones    String?
}

model User {
  id       String @id @default(uuid())
  name     String
  email    String @unique
  password String
}

model Colaboradores {
  id            String               @id @default(uuid())
  referenciales referencialesTable[]
  name          String
  email         String               @unique
  imageUrl      String
}
